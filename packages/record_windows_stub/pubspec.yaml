name: record_windows
description: Stub for record_windows - never runs, only satisfies Dart compiler
version: 1.0.7

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  record_platform_interface: ^1.0.0

flutter:
  plugin:
    platforms:
      windows:
        dartPluginClass: RecordWindows
